# Clearexcept command

Clear all tag fields except the ones you explicitly keep. This is useful when you want to preserve just a handful of fields and remove everything else.

Example:

```
:clearexcept title artist picture
```